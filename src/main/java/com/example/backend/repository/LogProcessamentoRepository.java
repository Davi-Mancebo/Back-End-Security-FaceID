package com.example.backend.repository;

import com.example.backend.model.LogProcessamentoModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LogProcessamentoRepository extends JpaRepository<LogProcessamentoModel, Long> {
}
